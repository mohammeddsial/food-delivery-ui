diff --git a/index.html b/index.html
index c372d33..444593c 100644
--- a/index.html
+++ b/index.html
@@ -1548,6 +1548,28 @@
             opacity: 1;
             transform: translateX(-50%) translateY(0);
         }
+
+        /* ΓöÇΓöÇΓöÇ Animated Active Nav Indicator ΓöÇΓöÇΓöÇ */
+        .nav-indicator {
+            position: absolute;
+            bottom: 0;
+            height: 3px;
+            border-radius: 3px;
+            transition: left 0.3s cubic-bezier(0.4, 0, 0.2, 1), width 0.3s cubic-bezier(0.4, 0, 0.2, 1);
+            pointer-events: none;
+        }
+        .theme-neumorphism .nav-indicator { background: #1A1A1A; }
+        .theme-glassmorphism .nav-indicator { background: var(--accent); }
+        .theme-neubrutalism .nav-indicator { background: var(--accent); box-shadow: 2px 2px 0 #000; }
+
+        /* ΓöÇΓöÇΓöÇ Modern / Neubrutalism Active Nav Contrast ΓöÇΓöÇΓöÇ */
+        .theme-neubrutalism .bottom-nav-item.active {
+            border: 3px solid #3D3229 !important;
+            font-weight: 900 !important;
+            border-radius: 12px;
+            background: var(--accent) !important;
+            color: #fff !important;
+        }
     </style>
 </head>
 
@@ -3069,24 +3091,33 @@
             window.selectedCatByTheme[theme] = cat || null;
             window.selectedItemByTheme = window.selectedItemByTheme || {};
             window.selectedItemByTheme[theme] = itemId || null;
+            // Sync active nav index based on page
+            const navPageMap = { 'feed': 0, 'menu-page': 1, 'rewards-page': 2, 'cart-page': 3, 'profile-settings': 4 };
+            if (navPageMap[page] !== undefined) {
+                activeNavIndex = navPageMap[page];
+            }
             renderAllViewports();
             renderPageViewPicker();
+            requestAnimationFrame(updateNavIndicators);
         }
         function goBack(theme) {
             const phase = window.appState.orderPhase[theme];
             if (phase === 'checkout') {
                 currentPageByTheme[theme] = 'cart-page';
                 window.appState.orderPhase[theme] = 'carted';
+                activeNavIndex = 3;
             } else if (phase === 'placed' || phase === 'tracking') {
                 currentPageByTheme[theme] = 'order-success-page';
             } else {
                 currentPageByTheme[theme] = 'feed';
                 window.appState.orderPhase[theme] = 'browsing';
+                activeNavIndex = 0;
             }
             window.selectedCatByTheme = window.selectedCatByTheme || {};
             window.selectedCatByTheme[theme] = null;
             renderAllViewports();
             renderPageViewPicker();
+            requestAnimationFrame(updateNavIndicators);
         }
         function proceedToCheckout(theme) {
             window.appState.orderPhase[theme] = 'checkout';
@@ -4025,6 +4056,24 @@
         function setActiveNav(index) {
             activeNavIndex = index;
             renderAllViewports();
+            requestAnimationFrame(updateNavIndicators);
+        }
+
+        function updateNavIndicators() {
+            document.querySelectorAll('.bottom-nav-bar').forEach(bar => {
+                const buttons = bar.querySelectorAll('.bottom-nav-item');
+                const indicator = bar.querySelector('.nav-indicator');
+                if (!indicator || !buttons.length) return;
+                const activeBtn = bar.querySelector('.bottom-nav-item.active');
+                if (activeBtn) {
+                    const barRect = bar.getBoundingClientRect();
+                    const btnRect = activeBtn.getBoundingClientRect();
+                    indicator.style.left = (btnRect.left - barRect.left + btnRect.width * 0.2) + 'px';
+                    indicator.style.width = (btnRect.width * 0.6) + 'px';
+                } else {
+                    indicator.style.width = '0px';
+                }
+            });
         }
 
         function generateBottomBar(theme, style = 'default') {
@@ -4095,7 +4144,7 @@
                     else if (item.icon === 'cart') clickHandler = `navigateToPage('${theme}','cart-page')`;
                     else if (item.icon === 'user') clickHandler = `navigateToPage('${theme}','profile-settings')`;
 
-                    return `<button onclick="${clickHandler}" style="display:flex;flex-direction:column;align-items:center;gap:2px;background:none;border:none;cursor:pointer;padding:6px 8px;position:relative;transition:transform 0.15s;" onmousedown="this.style.transform='scale(0.9)'" onmouseup="this.style.transform='scale(1)'" onmouseleave="this.style.transform='scale(1)'">${iconHtml}<span style="font-size:10px;${labelStyle}">${item.label}</span>${dotHtml}${badgeHtml}</button>`;
+                    return `<button class="bottom-nav-item${isActive ? ' active' : ''}" onclick="${clickHandler}" style="display:flex;flex-direction:column;align-items:center;gap:2px;background:none;border:none;cursor:pointer;padding:6px 8px;position:relative;transition:transform 0.15s;" onmousedown="this.style.transform='scale(0.9)'" onmouseup="this.style.transform='scale(1)'" onmouseleave="this.style.transform='scale(1)'">${iconHtml}<span style="font-size:10px;${labelStyle}">${item.label}</span>${dotHtml}${badgeHtml}</button>`;
                 }).join('');
 
                 let containerStyle;
@@ -4106,7 +4155,7 @@
                 } else {
                     containerStyle = `background:#FFFFFF; border-top:1.5px solid #E0E0E0; height:64px; display:flex; align-items:center; justify-content:space-around; padding:0 8px; z-index:30; box-shadow:0 -2px 8px rgba(0,0,0,0.04);`;
                 }
-                return `<div style="${containerStyle}">${itemsHtml}</div>`;
+                return `<div class="bottom-nav-bar" style="position:relative;${containerStyle}" data-theme="${theme}">${itemsHtml}<div class="nav-indicator" style="left:0;width:0;"></div></div>`;
             }
 
             // ΓöÇΓöÇΓöÇ VARIANT 2 (option 1): Floating glass pill ΓöÇΓöÇ
@@ -4132,13 +4181,13 @@
                     else if (item.icon === 'cart') clickHandler = `navigateToPage('${theme}','cart-page')`;
                     else if (item.icon === 'user') clickHandler = `navigateToPage('${theme}','profile-settings')`;
 
-                    return `<button onclick="${clickHandler}" aria-label="${item.label}" style="display:flex;flex-direction:column;align-items:center;gap:3px;background:none;border:none;cursor:pointer;padding:4px 6px;position:relative;transition:all 0.2s;${activePill}" onmousedown="this.style.transform='scale(0.93)'" onmouseup="this.style.transform='scale(1)'" onmouseleave="this.style.transform='scale(1)'"><div style="display:flex;align-items:center;justify-content:center;width:32px;height:32px;position:relative;">${iconHtml}${badgeHtml}</div></button>`;
+                    return `<button class="bottom-nav-item${isActive ? ' active' : ''}" onclick="${clickHandler}" aria-label="${item.label}" style="display:flex;flex-direction:column;align-items:center;gap:3px;background:none;border:none;cursor:pointer;padding:4px 6px;position:relative;transition:all 0.2s;${activePill}" onmousedown="this.style.transform='scale(0.93)'" onmouseup="this.style.transform='scale(1)'" onmouseleave="this.style.transform='scale(1)'"><div style="display:flex;align-items:center;justify-content:center;width:32px;height:32px;position:relative;">${iconHtml}${badgeHtml}</div></button>`;
                 }).join('');
 
                 let containerStyle = isG
                     ? `position:absolute; bottom:12px; left:16px; right:16px; height:58px; background:rgba(22,22,22,0.92); backdrop-filter:blur(20px); border:1px solid rgba(255,255,255,0.06); border-radius:24px; display:flex; align-items:center; justify-content:space-around; padding:0 8px; z-index:30;`
                     : `position:absolute; bottom:16px; left:16px; right:16px; height:56px; background:rgba(255,255,255,0.92); backdrop-filter:blur(16px); border:1px solid rgba(0,0,0,0.06); border-radius:24px; display:flex; align-items:center; justify-content:space-around; padding:0 8px; z-index:30; box-shadow:0 4px 24px rgba(0,0,0,0.08);`;
-                return `<div style="${containerStyle}">${itemsHtml}</div>`;
+                return `<div class="bottom-nav-bar" style="position:relative;${containerStyle}" data-theme="${theme}">${itemsHtml}<div class="nav-indicator" style="left:0;width:0;"></div></div>`;
             }
 
             // ΓöÇΓöÇΓöÇ VARIANT 3 (option 2): Bold bordered nav ΓöÇΓöÇ
@@ -4166,11 +4215,11 @@ let iconHtml = iconFor(theme, iconName, isActive ? 'font-size:20px;' : 'font-siz
                     else if (item.icon === 'cart') clickHandler = `setActiveNav(3);navigateToPage('${theme}','cart-page')`;
                     else if (item.icon === 'user') clickHandler = `setActiveNav(4);navigateToPage('${theme}','profile-settings')`;
 
-                return `<button onclick="${clickHandler}" aria-label="${item.label}" style="display:flex;flex-direction:column;align-items:center;gap:2px;background:none;border:none;cursor:pointer;padding:4px 6px;position:relative;transition:transform 0.12s;" onmousedown="this.style.transform='scale(0.92)'" onmouseup="this.style.transform='scale(1)'" onmouseleave="this.style.transform='scale(1)'"><div style="width:36px;height:36px;border-radius:10px;display:flex;align-items:center;justify-content:center;${btnBg}position:relative;transition:all 0.2s;">${iconHtml}${badgeHtml}</div></button>`;
+                return `<button class="bottom-nav-item${isActive ? ' active' : ''}" onclick="${clickHandler}" aria-label="${item.label}" style="display:flex;flex-direction:column;align-items:center;gap:2px;background:none;border:none;cursor:pointer;padding:4px 6px;position:relative;transition:transform 0.12s;" onmousedown="this.style.transform='scale(0.92)'" onmouseup="this.style.transform='scale(1)'" onmouseleave="this.style.transform='scale(1)'"><div style="width:36px;height:36px;border-radius:10px;display:flex;align-items:center;justify-content:center;${btnBg}position:relative;transition:all 0.2s;">${iconHtml}${badgeHtml}</div></button>`;
             }).join('');
 
             const containerStyle3 = `background:#F8F6F3; border-top:1.5px solid #3D3229; position:absolute; bottom:0; left:0; right:0; height:64px; display:flex; align-items:center; justify-content:space-around; padding:0 8px; z-index:30; box-shadow:0 -3px 0 #3D3229;`;
-            return `<div style="${containerStyle3}">${itemsHtml3}</div>`;
+            return `<div class="bottom-nav-bar" style="position:relative;${containerStyle3}" data-theme="${theme}">${itemsHtml3}<div class="nav-indicator" style="left:0;width:0;"></div></div>`;
         }
 
         function getCardText(moduleId, theme) {