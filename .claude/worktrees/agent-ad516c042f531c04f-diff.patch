diff --git a/index.html b/index.html
index c372d33..7f6ba17 100644
--- a/index.html
+++ b/index.html
@@ -174,6 +174,29 @@
             cursor: pointer;
             letter-spacing: 0.3px;
             transition: all 0.2s;
+            position: relative;
+            overflow: hidden;
+        }
+        .neu-add-btn::after,
+        .glass-add-btn::after,
+        .brutal-add-btn::after {
+            content: '';
+            position: absolute;
+            width: 30px;
+            height: 30px;
+            background: rgba(255,255,255,0.3);
+            border-radius: 50%;
+            transform: scale(0);
+            pointer-events: none;
+            top: 50%;
+            left: 50%;
+            margin-top: -15px;
+            margin-left: -15px;
+        }
+        .neu-add-btn:active::after,
+        .glass-add-btn:active::after,
+        .brutal-add-btn:active::after {
+            animation: rippleEffect 0.5s ease-out;
         }
 
         /* --- THEME 2: GLASSMORPHISM (Remix Icon) --- */
@@ -312,6 +335,12 @@
             font-size: 12px;
             font-weight: 600;
             cursor: pointer;
+            position: relative;
+            overflow: hidden;
+        }
+        .glass-add-btn {
+            position: relative;
+            overflow: hidden;
         }
 
         .brutal-badge {
@@ -1298,6 +1327,25 @@
         /* Button Morph Animation */
         .btn-morph {
             transition: all 0.3s cubic-bezier(0.34, 1.56, 0.64, 1);
+            position: relative;
+            overflow: hidden;
+        }
+        .btn-morph::after {
+            content: '';
+            position: absolute;
+            width: 30px;
+            height: 30px;
+            background: rgba(255,255,255,0.3);
+            border-radius: 50%;
+            transform: scale(0);
+            pointer-events: none;
+            top: 50%;
+            left: 50%;
+            margin-top: -15px;
+            margin-left: -15px;
+        }
+        .btn-morph:active::after {
+            animation: rippleEffect 0.5s ease-out;
         }
 
         .btn-morph:hover {
@@ -1548,6 +1596,71 @@
             opacity: 1;
             transform: translateX(-50%) translateY(0);
         }
+
+        /* ΓöÇΓöÇΓöÇ INTERACTION FEEDBACK ΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇ */
+
+        /* 1. Button Ripple Effect */
+        @keyframes rippleEffect {
+            0% { transform: scale(0); opacity: 0.4; }
+            100% { transform: scale(4); opacity: 0; }
+        }
+        .btn-ripple {
+            position: relative;
+            overflow: hidden;
+        }
+        .btn-ripple::after {
+            content: '';
+            position: absolute;
+            width: 30px;
+            height: 30px;
+            background: rgba(255,255,255,0.3);
+            border-radius: 50%;
+            transform: scale(0);
+            pointer-events: none;
+        }
+        .btn-ripple:active::after {
+            animation: rippleEffect 0.5s ease-out;
+        }
+
+        /* 2. Cart Badge Bounce */
+        @keyframes cartBounce {
+            0% { transform: scale(1); }
+            30% { transform: scale(1.4); }
+            60% { transform: scale(0.9); }
+            100% { transform: scale(1); }
+        }
+        .cart-bounce {
+            animation: cartBounce 0.4s ease;
+        }
+
+        /* 3. Skeleton Shimmer Loading Placeholder */
+        .skeleton-shimmer {
+            background: linear-gradient(90deg, rgba(200,200,200,0.1) 25%, rgba(200,200,200,0.3) 50%, rgba(200,200,200,0.1) 75%);
+            background-size: 200% 100%;
+            animation: shimmer 1.5s infinite;
+        }
+
+        /* 4. Press States for Interactive Cards */
+        .neu-food-card:active,
+        .glass-food-card:active,
+        .brutal-food-card:active,
+        .glass-card:active,
+        .brutal-card:active,
+        .neu-flat:active {
+            transform: scale(0.98);
+            transition: transform 0.1s ease;
+        }
+
+        /* 5. Page Transition */
+        .page-fade-enter {
+            opacity: 0;
+            transform: translateY(8px);
+            transition: opacity 0.25s ease, transform 0.25s ease;
+        }
+        .page-fade-active {
+            opacity: 1;
+            transform: translateY(0);
+        }
     </style>
 </head>
 
@@ -1877,6 +1990,12 @@
             }
             showToast(`${item.name} added to cart`);
             renderAllViewports();
+            // Bounce all cart badges for this theme
+            const fabBadge = document.getElementById(`fabCartBadge-${theme}`);
+            if (fabBadge) {
+                fabBadge.classList.add('cart-bounce');
+                setTimeout(() => fabBadge.classList.remove('cart-bounce'), 400);
+            }
         }
 
         function removeFromCart(theme, itemId) {
@@ -3461,7 +3580,15 @@
                     scrollWrapper.innerHTML += html;
                 });
 
+                // Page fade-in transition
+                scrollWrapper.classList.add('page-fade-enter');
                 container.appendChild(scrollWrapper);
+                requestAnimationFrame(() => {
+                    scrollWrapper.classList.add('page-fade-active');
+                    scrollWrapper.addEventListener('transitionend', () => {
+                        scrollWrapper.classList.remove('page-fade-enter', 'page-fade-active');
+                    }, { once: true });
+                });
 
                 // Init video dot navigation
                 initVideoDots(theme);